【解決】
//座席状況の有無（判別処理）
まずSQLで予約番号を指定する
そのデータがあればCssClassを"SoldSeat"とクリックできなくする
それをfor文か何かで回し全データチェックする

６席目クリック時のオンロードのメッセージ表示するときにデザインが乱れる

席をクリックしたとき、デザインがちらつくことがある

「案」
JSでポストバック処理をすればサーバーに返さずweb上で処理できるらしい
書き方がわからない

出来るが仮フォーム作成してそこで情報を残してそのフォームからASPに情報を上げる
つまり仮フォーム経由

---------------------------------
【未解決】
スケジュール画面でエリアをクリックして座席画面に移動してそのエリアを拡大表示する。
【方法】
エリア数だけのif文を用意して、拡大と座標を設定する

【SQL】
座席
予約


エリア全数の座席
SELECT SEAT_AREA,COUNT(SEAT_AREA) FROM TBL_SEAT WHERE SCREEN_ID GROUP BY SEAT_AREA;





